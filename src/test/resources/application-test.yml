spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test.deckassemble.app/
          audiences: test-audience
  liquibase:
    enabled: true

logging:
  level:
    liquibase: DEBUG
    org.springframework.jdbc: DEBUG
